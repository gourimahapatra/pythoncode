myList = [1,3,4,5,6,79,9]

if 4 in myList:
    print("Found")
else:
    print("Not found")

def lSearch(lst, target):
    for i in range(len(lst)):
        if lst[i] == target:
            return i
        
    return -1

print(lSearch(myList, 5))